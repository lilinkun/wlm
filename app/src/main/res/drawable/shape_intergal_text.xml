<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色(白色) -->
    <!--<stroke-->
    <!--android:width="1dp"-->
    <!--android:color="#ffffff" />-->
    <!-- 填充的颜色(白色) -->
    <solid android:color="@color/hot_integral" />
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="25dp" />

    <padding android:top="0dp" android:bottom="0dp" android:right="@dimen/padding_10" android:left="@dimen/padding_10"/>


</shape>